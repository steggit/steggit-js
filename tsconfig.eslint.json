{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    // ensure that nobody can accidentally use this config for a build
    "noEmit": true,
  },
  "include": [
    "**/*.ts",
    "**/*.js",
    "**/*.d.ts",
    "**/*.mjs",
    "**/*.cjs",
  ],
}