#include "unity.h"

void setUp(void) {
  // Optional: Called before every test
}

void tearDown(void) {
  // Optional: Called after every test
}

int main(void) {
  UNITY_BEGIN();

  return UNITY_END();
}